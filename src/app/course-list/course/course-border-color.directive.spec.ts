import { CourseBorderColorDirective } from './course-border-color.directive';

describe('CourseBorderColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseBorderColorDirective();
    expect(directive).toBeTruthy();
  });
});
